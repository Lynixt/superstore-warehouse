name: "superstore"
version: "1.0.0"
config-version: 2

profile: "superstore"

models:
  superstore:
    bronze:
      +materialized: view
    silver:
      +materialized: table
    gold:
      +materialized: table

seeds:
  superstore:
    +column_types:
      order_date: DATE
      unit_price: NUMERIC
      quantity: INTEGER
      discount: NUMERIC
